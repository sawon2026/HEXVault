# HEXVault Web Dashboard (v1.3)

Next.js 14 + Tailwind. Connects to HEXVault REST API.

## Run

```bash
# Terminal 1 (repo root)
npm run api

# Terminal 2
cd apps/web && npm install && npm run dev
# http://localhost:3000
```

Pages: `/` Overview · `/memories` · `/search` · `/chat` · `/analyze`
