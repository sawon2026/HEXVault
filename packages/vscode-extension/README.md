# HEXVault for VS Code

## Features
- Sidebar panel — Search / Ask / Health
- Command Palette: Search, Add Selection, Ask, Health
- Editor context menu for selection

## Develop
```bash
cd packages/vscode-extension
npm install && npm run compile
# F5 in VS Code
```

Requires `npm run api` on http://127.0.0.1:3850
